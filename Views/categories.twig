{% extends 'base.twig' %}

{% block content %}
  <section class="categories">
    {% for category in categories %}
      <a class="cat" href="/?cat_id={{ category.id }}">{{ category.nom }}</a>
    {% endfor %}
  </section>
{% endblock %}
